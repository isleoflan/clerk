import { IntCurrencyPipe } from './int-currency.pipe';

describe('IntCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new IntCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
